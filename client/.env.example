# Frontend API base URL. Leave empty for same-origin (Vite dev proxy handles localhost).
# For Capacitor production builds, set to the deployed API origin:
# VITE_API_BASE_URL=https://iht-performance-app.onrender.com
VITE_API_BASE_URL=
